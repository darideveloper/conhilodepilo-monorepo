# Tasks: Refactor components to slots

- [x] Create/Update component specs to include slot requirements.
- [x] Refactor `SectionHeader.astro` to use slots for `title` and `subtitle`.
- [x] Refactor `InfoCard.astro` to use a slot for `text`.
- [x] Refactor `ServiceCard.astro` to use a slot for `description`.
- [x] Refactor `CourseCard.astro` to use a slot for `description`.
- [x] Update all usages of these components in the codebase.
- [x] Verify that all components render correctly with both props (if still supported) and slots.
