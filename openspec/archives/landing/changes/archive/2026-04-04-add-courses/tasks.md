## 1. Implementation
- [x] 1.1 Download the 3 course images into `src/assets/images/courses/`
- [x] 1.2 Scaffold `CourseCard.astro`, passing in props like `title`, `duration`, `level`, `description`, and `image`.
- [x] 1.3 Scaffold `CoursesSection.astro` using `<SectionHeader />` logic instead of raw HTML headers.
- [x] 1.4 Map the exact texts and imported local images from the HTML design into the components seamlessly matching the design tokens (`border-brand-primary/10`, `bg-brand-primary/10` equivalents).
- [x] 1.5 Add `<CoursesSection />` to `src/pages/index.astro`
