# Implementation Tasks

- [x] 1. **Update CategoryCard Buttons:** Add `cursor-pointer` to the nested service selection buttons in `src/components/organisms/CategoryCard.tsx`.
- [x] 2. **Update CategoryCard CTA:** Add `cursor-pointer` to the "Reservar" button in `src/components/organisms/CategoryCard.tsx`.
- [x] 3. **Verify Interactive Elements:** Check `CategoryShowcase.tsx` and ensure any future interactable wrappers are correctly styled.
