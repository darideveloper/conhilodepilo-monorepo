# Tasks: Install and setup Tailwind CSS v4

- [x] Run `npx astro add tailwind` to install and configure the integration.
- [x] Verify `package.json` for `tailwindcss` v4 and `@tailwindcss/vite`.
- [x] Create `src/styles/global.css` with `@import "tailwindcss";` if not already present.
- [x] Update `src/layouts/Layout.astro` to import `src/styles/global.css`.
- [x] Verify Tailwind setup with a test component or utility class in `src/pages/index.astro`.
