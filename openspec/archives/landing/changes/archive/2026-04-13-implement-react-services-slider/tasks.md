# Tasks: Implement React Services Slider

1. [x] **Create React Service Card:** Create `src/components/molecules/ServiceCard.tsx` that visually matches the existing `ServiceCard.astro`.
2. [x] **Build Services Slider:** Create `src/components/organisms/ServicesSlider.tsx` implementing `swiper` and state-based category filtering.
3. [x] **Integrate to Design System:** Add the new `ServicesSlider` component to `src/pages/design-system.astro` and pass the available services data for testing.
4. [x] **Test & Validate:** Ensure filtering updates the slides correctly and Swiper handles pagination/navigation gracefully without breaking the build.