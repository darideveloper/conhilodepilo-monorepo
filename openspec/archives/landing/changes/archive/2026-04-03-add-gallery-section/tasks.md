## 1. Implementation
- [x] 1.1 Extract and download the 8 image URLs from `.themes/code.html` into `src/assets/images/gallery/`.
- [x] 1.2 Create `src/components/Gallery.astro` with the grid mosaic structure from `.themes/code.html`.
- [x] 1.3 Map the provided layout to the design system (tokens like `bg-brand-primary/5`, `text-brand-secondary`, `<SectionHeader />`).
- [x] 1.4 Structure the `Gallery` component to accept props/data dynamically to prepare for a future API integration, using the downloaded local images as the initial dataset.
- [x] 1.5 Add `<Gallery />` to `src/pages/index.astro` directly below the `<ServicesGrid />`.
- [x] 1.6 Verify the mosaic responsive styling.
