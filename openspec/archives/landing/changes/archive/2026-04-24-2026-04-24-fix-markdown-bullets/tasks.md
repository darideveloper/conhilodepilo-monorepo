# Tasks

- [x] Create `.markdown` class in `src/styles/global.css` with list style and padding rules.
- [x] Update `src/pages/booking/[id].astro` to wrap the rendered markdown in `<div class="markdown">...</div>`.
- [x] Validate alignment and list rendering on the booking page.
