# Tasks: Implement Service Availability API

- [x] Implement `utils/availability.py` with core logic (batch fetch models, calculate availability window).
- [x] Implement `AvailabilityView` in `booking/views.py`.
- [x] Register `api/availability/days/` in `project/urls.py`.
- [x] Add unit tests in `booking/tests_api.py` covering positive (free slots) and negative (fully booked/unavailable) cases.
- [x] Validate tests pass and endpoint responds correctly.
