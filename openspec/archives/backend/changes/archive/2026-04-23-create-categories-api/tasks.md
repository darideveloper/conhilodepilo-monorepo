# Implementation Tasks

- [x] **Serializers:** Add `EventSerializer` and `EventTypeSerializer` to `booking/serializers.py` to define the response structure and model mapping.
- [x] **View:** Implement `ServicesListView` in `booking/views.py` using `APIView` for `GET /api/services/`.
- [x] **Routing:** Register the `ServicesListView` in `project/urls.py` under the `/api/services/` path.
- [x] **Tests:** Add test coverage for `ServicesListView` in `booking/tests_api.py` to verify response structure and data inclusion.
- [x] **Bruno:** Add `bruno/Booking/Get Services.yml` to the API collection.
