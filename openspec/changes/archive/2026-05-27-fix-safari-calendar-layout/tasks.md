## 1. Calendar Component Update

- [x] 1.1 Locate the `month` class definition in `booking/src/components/atoms/ui/calendar.tsx`.
- [x] 1.2 Update the `month` classes to include `grid` and ensure layout consistency.
- [x] 1.3 Verify if `flex flex-col` needs to be removed or adjusted when `grid` is added.

## 2. Verification

- [x] 2.1 Verify the layout in the local development environment.
- [x] 2.2 Ensure the calendar remains functional and properly aligned across different viewports.
