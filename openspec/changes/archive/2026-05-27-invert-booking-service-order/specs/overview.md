# Specs Overview

This change is a visual-only CSS reorder on Step 1 of the booking wizard. No requirements are added, modified, or removed from the existing specs.

- **No new capabilities** — no new specs created
- **No modified capabilities** — no spec-level requirements changed (existing `booking-ui` requirements are unaffected)

The entire change is confined to implementation (CSS `order` class additions in `BookingServiceSelection.tsx`).
