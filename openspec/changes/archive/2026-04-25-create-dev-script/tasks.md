# Tasks: Create Development Orchestration Script

- [x] Create `dev.sh` script in the root directory.
- [x] Configure `dev.sh` to use `tmux` for backend (Django) and frontends (Astro).
- [x] Ensure backend script uses the existing `venv`.
- [x] Make `dev.sh` executable.
- [x] Verify local startup sequence.
