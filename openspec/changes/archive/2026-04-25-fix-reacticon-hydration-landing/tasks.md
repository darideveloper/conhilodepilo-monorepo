# Tasks

- [x] **Add `client:load` to `ReactIcon` in `landing/src/pages/booking/[id].astro`**
    Find all usages of `<ReactIcon` in the file and add the `client:load` directive to each instance.
    **Validation**: Start the landing service dev server, visit a booking detail page, and verify that the icons (time, back arrow, WhatsApp) render correctly.