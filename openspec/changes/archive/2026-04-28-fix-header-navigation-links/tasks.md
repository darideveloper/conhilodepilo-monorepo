# Tasks: Fix Header Navigation Links

- [x] 1.1 Update `landing/src/components/organisms/Header.astro` navigation links to include the root path (`/` before the hash) so they work across all subpages.